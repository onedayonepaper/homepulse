services:
  homepulse:
    build: .
    container_name: homepulse
    restart: unless-stopped
    ports:
      - "8787:8787"
    env_file:
      - .env
    volumes:
      - ./data:/data
      - ./devices.json:/app/devices.json:ro
